{
  "name": "debugrunner",
  "private": true,
  "version": "0.3.1",
  "type": "module",
  "description": "DebugRunner - meta debug 2D action prototype (Phase 1 vertical slice)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "vite": "^5.4.10"
  }
}
